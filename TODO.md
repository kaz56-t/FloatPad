# TODO

## Memo タブ

- [x] **複数メモ** — タブで切り替え、追加（`+`）・削除（`×`）・名前変更（dblclick + `prompt()`）・起動時に最後のメモを復元
- [ ] **メモ名の変更 inline化** — `prompt()` から タブ上でその場編集（`contenteditable` or `<input>`）に置き換え
- [ ] **箇条書き・インデント対応** — `Tab` キーでインデント、`-` や `*` で自動箇条書き、行頭記号をツールバーで挿入

## 新規タブ

- [ ] **定型文タブ** — よく使うテキストを登録し、ボタン一押しでクリップボードにコピー
  - 定型文の追加 / 編集 / 削除
  - 並び替え（ドラッグ or 上下ボタン）
  - 定型文は `user-data/snippets.json` に保存

## ウィンドウ

- [ ] **最小化ボタン** — タイトルバーに `─` ボタンを追加（`win.minimize()` via IPC）
